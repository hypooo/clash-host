server {
    listen 9080;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    location /xd/ {
        alias /usr/share/nginx/html/xd/;
        try_files $uri $uri/ /index.html;
    }

}
